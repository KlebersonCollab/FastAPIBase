---
alwaysApply: true
---
📜 Rule: FastAPI como Django — Automação e Produtividade
1. Estrutura e Organização
Siga a estrutura modular inspirada no Django:
core/: Configurações globais, settings, inicialização do app.
api/: Routers (controllers/views).
services/: Lógica de negócio (use cases).
repositories/: Abstração de dados (ORM, queries).
schemas/: DTOs e validação (Pydantic).
models/: Modelos ORM (SQLAlchemy).
Cada app/feature pode ser um subpacote, como apps Django.
2. Automação de CRUD e Admin
Implemente um gerador de CRUD automático (como o Django Admin):
Use reflection dos modelos para criar endpoints CRUD e validação automaticamente.
Gere rotas, schemas e permissões a partir dos modelos.
Crie um painel administrativo web (pode usar FastAPI Admin, Tortoise-Admin ou customização).
3. Migrações e ORM
Use SQLAlchemy 2.0 (async) como ORM padrão.
Utilize Alembic para migrações automáticas, integrando comandos de migração ao CLI do projeto.
Gere modelos e migrações a partir de schemas (Contract-First).
4. CLI de Gerenciamento
Crie um CLI (ex: manage.py ou cli.py) para:
Rodar servidor, criar apps, rodar migrações, criar superusuário, popular dados, etc.
Comandos devem ser automáticos e interativos, como no Django.
5. Autenticação, Permissões e Usuários
Implemente autenticação JWT e OAuth2.
Gere modelos de usuário, permissões e grupos automaticamente.
Forneça endpoints para login, registro, recuperação de senha, etc.
6. Configuração e Settings
Use Pydantic para settings, com carregamento automático de .env por ambiente.
Separe settings por ambiente (dev, prod, test).
7. Testes e Fixtures
Estruture testes por app/feature.
Use pytest, pytest-asyncio e factory_boy/model_bakery para fixtures.
Gere testes básicos automaticamente ao criar novos modelos/endpoints.
8. Documentação e OpenAPI
Documentação automática via OpenAPI/Swagger.
Gere exemplos e descrições a partir dos schemas e docstrings.
9. Segurança e Boas Práticas
Implemente CORS, rate limiting, CSRF, proteção de headers e validação de entrada.
Gere middlewares de segurança automaticamente.
10. Extensibilidade e Plugins
Permita a criação de plugins/apps reutilizáveis, como no Django.
Facilite a integração de apps de terceiros (ex: autenticação social, admin, etc).
Exemplo de Automação Esperada
uv run cli.py startapp blog → Cria estrutura de app, modelos, rotas, testes, admin.
uv run cli.py makemigrations → Detecta mudanças nos modelos e gera migrações.
uv run cli.py createsuperuser → Cria usuário admin via prompt.
CRUD, permissões e admin web prontos para cada modelo criado.
